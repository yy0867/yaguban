<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.야구반기록관리" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- App Font -->
        <item name="android:fontFamily">@font/bccardlight</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main_color</item>
        <item name="colorPrimaryVariant">@color/status_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    
    <style name="standard_font_style_bold" parent="android:Widget">
        <item name="android:fontFamily">@font/bccardbold</item>
    </style>

    <style name="standard_font_style_light" parent="android:Widget">
        <item name="android:fontFamily">@font/bccardlight</item>
    </style>
</resources>